name: Snowflake_dbt_POC
version: 1.0.0
config-version: 2
profile: Snowflake_dbt_POC
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  Snowflake_dbt_POC:
    +materialized: view
